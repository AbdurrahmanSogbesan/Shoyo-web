const icons = {
  cart: require("./cart").default,
  arrow: require("./arrow").default,
  nextArrow: require("./nextArrow").default,
  prevArrow: require("./prevArrow").default,
};

export default icons;
